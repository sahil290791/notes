<%= form_for(@note, class: 'form') do |f| %>
  <% if @note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@note.errors.count, "error") %> prohibited this note from being saved:</h2>

      <ul>
      <% @note.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :title %>
    <%= f.text_field :title,class: 'form-control',:placeholder =>"Title in 30 words", maxlength: '30' %>
  </div>
  
  <div class="form-group">
    <%= f.label :description %>
    <%= f.text_area :description,class: 'form-control',:placeholder=> "Describe your note.." %>
  </div>

  <div class="form-group">
    <%= f.submit "Save", class:'btn btn-sm btn-success' %>
  </div>
<% end %>
